# socer - это...
socer - это инструмент, предоставляющий функционал для удобного обогащения и расследования инцидентов
# todo
- Process Journey
    - Переписать логику `buildGraph()` и файла `graph-helpers.js` в целом
    - Сделать ID нод более комплексными (например `timestamp:pid:fullpath`)
    - Улучшить считывание параметров парсинга, учесть ситуации, когда некоторых значений может не хватать (например, не в каждом событии может быть `subject.process.parent.fullpath` и аналогичные значения)
    - (?) Сделать обертку над объектом, полученным от PapaParse при парсинге CSV-файла.
    - Фильтры для нод
        - "Показать только ноду X и всех ее детей"
        - "Путь от корня до ноды X"
        - "Полный путь от корня до листка, который содержит ноду X"
        - Комбинации фильтров
- Тулбокс (?multis/multic/multitool)
    - Автоопределение объекта (IPv4, IPv6, MAC, domain, URL, SSL, Email, Hash, Phone, и тд)
        - Учесть возможность вставки зашумленных значений
    - Получение хеша по файлу (опять же все локально)
    - Предоставление для объекта ресурсов для анализа, в том числе с подстановками, где это возможно
        - Например, для URL: `https://web.archive.org/web/{url}`
        - Hash для VirusTotal: `https://www.virustotal.com/gui/file/{hash}`
        - Однако на VirusTotal поиск по URL, который ранее не сканировался не возможен, в таком случае нужно тоже учитывать эти моменты
        - Также стоит учесть, что некоторые ресурсы требуют регистрации
    - Зашумление/разшумление (дефангирование/рефангирование)
    - Более специфические вещи
        - ip2dec => `from <= {ip_dec} and to >= {ip_dec}`
- Генератор/сборник рецептов для CyberChef
    - Defang/Refang
    - Строки -> Список
    - Строки -> Более комплексный объект (например, `X` -> `body contains "X"`)
    - Создание комбинаций
- ASCII-tree Generator